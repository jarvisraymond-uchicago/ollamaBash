 ```typescript
import { render, fireEvent } from '@testing-library/react';
import ABC from './ABC'; // Assuming the file is named 'ABC.tsx' and located in the same directory

describe('ABC component', () => {
  it('renders "ABC"', () => {
    const { getByTestId } = render(<ABC data-testid="component" />);
    expect(getByTestId('component')).toHaveTextContent('ABC');
  });
});
```

