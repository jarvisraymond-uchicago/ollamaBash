NOTES


OUTPUT=$(ls -1)
echo "${OUTPUT}"


FILECONTENTS =$(cat TableRow.tsx)
file_contents=$(awk 'BEGIN {print $0}' my_file.txt)
file_contents=$(echo "my_file.txt")
echo "$file_contents"


ollama run codellama "write a unit test for this file ${FILECONTENTS}"

